<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->
    <!-- Deliberately minimal - only what MeterSahi's actual features
         use. No camera, storage, contacts, phone, or SMS permissions,
         since the app has no features that need them. -->

    <!-- Required for all network access: loading the site itself,
         Google Maps/Routes/Places API, Supabase, Google Analytics. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Lets the app check connectivity status (e.g. to show a
         friendlier state than a raw failed-request error when the
         device is offline). -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Required for the "use my current location" pickup-field
         feature (attemptAutoLocate() in app.js, calling the browser's
         navigator.geolocation.getCurrentPosition() inside the
         WebView). Both fine and coarse are requested since the web
         Geolocation API itself doesn't distinguish accuracy tiers -
         Android grants whichever the user allows, and the site's
         existing code already handles a denied/unavailable location
         gracefully (falls back to manual address entry, no crash,
         no forced permission). -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
</manifest>
