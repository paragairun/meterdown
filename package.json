{
  "name": "metersahi-astro",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "extract-cities": "node scripts/extract-cities.js",
    "generate-sitemap": "node scripts/generate-sitemap.js",
    "predev": "npm run extract-cities && npm run generate-sitemap",
    "prebuild": "npm run extract-cities && npm run generate-sitemap",
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "astro": "^7.0.7",
    "@capacitor/android": "^8.4.2",
    "@capacitor/core": "^8.4.2"
  },
  "devDependencies": {
    "vitest": "4.1.10",
    "@capacitor/cli": "^8.4.2"
  }
}
